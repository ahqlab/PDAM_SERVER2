<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.octacomm.sample.dao.mapper.FranchiseMapper">

	<sql id="condition">
        <where>
        	isDel = 0
        	<if test="param.searchField != null and param.searchField != '' and param.searchWord != null and param.searchWord != ''">
       			and ${param.searchField} like '%${param.searchWord}%'
       		</if>
       </where>
	</sql>
	
	<select id="getCountByParam" resultType="_int">
        SELECT COUNT(*) FROM TB_FRANCHISE
    	<include refid="condition"/>
    </select>
   
    <select id="getListByParam" resultType="Franchise">
		SELECT 
		  *
		FROM TB_FRANCHISE
      	<include refid="condition"/>
		ORDER BY idx DESC
        LIMIT #{pageSize} OFFSET #{startRow}
    </select>   
    
    <!-- 
    <select id="getCountByParam" resultType="_int">
        SELECT count(*) FROM (
			SELECT
				idx, 0 AS constructionIdx, fcName, userId, password, role, isDel, createDate, lastModifiedDate
			FROM TB_FRANCHISE
			UNION ALL
			SELECT
				idx, constructionIdx, userName, userId, password, role, isDel, createDate, lastModifiedDate
			FROM TB_MOU_USER
		) A 
    	<include refid="condition"/>
    </select>
   
    <select id="getListByParam" resultType="Franchise">
		SELECT A.* FROM (
			SELECT
				idx, 0 AS constructionIdx, fcName, userId, password, role, isDel, createDate, lastModifiedDate
			FROM TB_FRANCHISE
			UNION ALL
			SELECT
				idx, constructionIdx, userName, userId, password, role, isDel, createDate, lastModifiedDate
			FROM TB_MOU_USER
		) A 
      	<include refid="condition"/>
		ORDER BY A.idx DESC
        LIMIT #{pageSize} OFFSET #{startRow}
    </select>   -->
    
</mapper>