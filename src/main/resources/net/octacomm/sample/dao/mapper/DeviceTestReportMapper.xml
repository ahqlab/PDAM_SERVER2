<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.octacomm.sample.dao.mapper.DeviceTestReportMapper">

	<sql id="condition">
        <where>
        	<if test="param.searchField != null and param.searchField != '' and param.searchWord != null and param.searchWord != ''">
       			and ${param.searchField} like '%${param.searchWord}%'
       		</if>
       		<choose>
       			<when test="param.searchMfr != null and param.searchMfr != ''">
       				AND mfr = #{param.searchMfr}
       			</when>
       			<otherwise>
       				AND mfr = 'SOKKIA'
       			</otherwise>
       		</choose>
       </where>
	</sql>
	
	<select id="getCountByParam" resultType="_int">
        SELECT COUNT(*) FROM TB_DEVICE_TEST_REPORT
    	<include refid="condition"/>
    </select>
   
    <select id="getListByParam" resultType="DeviceTestReport">
		SELECT
			@rownum := @rownum + 1 AS no ,  
		 	a.*
		FROM TB_DEVICE_TEST_REPORT a, (select @rownum:=#{startRow}) temp
      	<include refid="condition"/>
		ORDER BY CAST(SUBSTRING_INDEX(fileName, ')', 1) as UNSIGNED) asc
        LIMIT #{pageSize} OFFSET #{startRow}
    </select>   
    
</mapper>