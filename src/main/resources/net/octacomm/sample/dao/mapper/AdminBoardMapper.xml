<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.octacomm.sample.dao.mapper.AdminBoardMapper">

    <select id="getBoardTotalCount" parameterType="map" resultType="int">
        SELECT COUNT(id)
        FROM TB_ADMIN_BOARD
        WHERE delete_at IS NULL
        <if test="keyword != null and keyword != ''">
            <choose>
                <when test="searchType == 'boardName'">
                    AND board_name LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <when test="searchType == 'auth'">
                    AND auth = #{keyword}
                </when>
                <otherwise>
                    AND board_name LIKE CONCAT('%', #{keyword}, '%')
                </otherwise>
            </choose>
        </if>
    </select>

    <select id="getBoardList" parameterType="map" resultType="net.octacomm.sample.domain.AdminBoard">
        SELECT 
            id, 
            board_name AS boardName, 
            use_yn AS useYn,
            auth, 
            allowed_exts AS allowedExts, 
            reg_date AS regDate,
            update_at AS updateAt
        FROM TB_ADMIN_BOARD
        WHERE delete_at IS NULL
        <if test="keyword != null and keyword != ''">
            <choose>
                <when test="searchType == 'boardName'">
                    AND board_name LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <when test="searchType == 'auth'">
                    AND auth = #{keyword}
                </when>
                <otherwise>
                    AND board_name LIKE CONCAT('%', #{keyword}, '%')
                </otherwise>
            </choose>
        </if>
        ORDER BY id DESC
        LIMIT #{startRow}, #{size}
    </select>

    <select id="getBoard" parameterType="long" resultType="net.octacomm.sample.domain.AdminBoard">
        SELECT 
            id, 
            board_name AS boardName, 
            use_yn AS useYn,
            auth, 
            allowed_exts AS allowedExts, 
            reg_date AS regDate,
            update_at AS updateAt
        FROM TB_ADMIN_BOARD
        WHERE id = #{id}
          AND delete_at IS NULL
    </select>

    <insert id="insertBoard" parameterType="net.octacomm.sample.domain.AdminBoard" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO TB_ADMIN_BOARD (
            board_name, 
            use_yn, 
            auth, 
            allowed_exts, 
            reg_date
        ) VALUES (
            #{boardName}, 
            #{useYn}, 
            #{auth}, 
            #{allowedExts}, 
            NOW()
        )
    </insert>

    <update id="updateBoard" parameterType="net.octacomm.sample.domain.AdminBoard">
        UPDATE TB_ADMIN_BOARD
        SET board_name = #{boardName},
            use_yn = #{useYn},
            auth = #{auth},
            allowed_exts = #{allowedExts},
            update_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="deleteBoard" parameterType="long">
        UPDATE TB_ADMIN_BOARD
        SET delete_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>