<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.octacomm.sample.dao.mapper.AdminBoardMapper">

    <select id="getBoardTotalCount" parameterType="map" resultType="int">
        SELECT COUNT(id)
        FROM TB_ADMIN_BOARD
        WHERE delete_at IS NULL
        <if test="keyword != null and keyword != ''">
            <choose>
                <when test="searchType == 'title'">
                    AND title LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <when test="searchType == 'content'">
                    AND content LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <when test="searchType == 'regUser'">
                    AND reg_user LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <otherwise>
                    AND title LIKE CONCAT('%', #{keyword}, '%')
                </otherwise>
            </choose>
        </if>
    </select>

    <select id="getBoardList" parameterType="map" resultType="net.octacomm.sample.domain.AdminBoard">
        SELECT 
            id, 
            title, 
            content,
            file_name AS fileName, 
            file_size AS fileSize, 
            reg_user AS regUser, 
            reg_date AS regDate,
            update_at AS updateAt
        FROM TB_ADMIN_BOARD
        WHERE delete_at IS NULL
        <if test="keyword != null and keyword != ''">
            <choose>
                <when test="searchType == 'title'">
                    AND title LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <when test="searchType == 'content'">
                    AND content LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <when test="searchType == 'regUser'">
                    AND reg_user LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <otherwise>
                    AND title LIKE CONCAT('%', #{keyword}, '%')
                </otherwise>
            </choose>
        </if>
        ORDER BY id DESC
        LIMIT #{startRow}, #{size}
    </select>

    <select id="getBoard" parameterType="long" resultType="net.octacomm.sample.domain.AdminBoard">
        SELECT 
            id, 
            title, 
            content,
            file_name AS fileName, 
            file_size AS fileSize, 
            reg_user AS regUser, 
            reg_date AS regDate,
            update_at AS updateAt
        FROM TB_ADMIN_BOARD
        WHERE id = #{id}
          AND delete_at IS NULL
    </select>

    <insert id="insertBoard" parameterType="map" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO TB_ADMIN_BOARD (
            title, 
            content, 
            file_name, 
            file_data, 
            file_size, 
            content_type, 
            reg_user, 
            reg_date
        ) VALUES (
            #{title}, 
            #{content}, 
            #{fileName}, 
            #{fileData, javaType=[B, jdbcType=BLOB}, 
            #{fileSize}, 
            #{contentType}, 
            #{regUser}, 
            NOW()
        )
    </insert>

    <update id="updateBoard" parameterType="map">
        UPDATE TB_ADMIN_BOARD
        SET title = #{title},
            content = #{content},
            reg_user = #{regUser},
            update_at = NOW()
            <if test="fileName != null and fileName != ''">
                , file_name = #{fileName}
                , file_data = #{fileData, javaType=[B, jdbcType=BLOB}
                , file_size = #{fileSize}
                , content_type = #{contentType}
            </if>
        WHERE id = #{id}
    </update>

    <select id="getFile" parameterType="long" resultType="net.octacomm.sample.domain.AdminBoard">
        SELECT 
            id, 
            file_name AS fileName, 
            file_data AS fileData, 
            content_type AS contentType
        FROM TB_ADMIN_BOARD
        WHERE id = #{id}
          AND delete_at IS NULL
    </select>

    <update id="deleteBoard" parameterType="long">
        UPDATE TB_ADMIN_BOARD
        SET delete_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>