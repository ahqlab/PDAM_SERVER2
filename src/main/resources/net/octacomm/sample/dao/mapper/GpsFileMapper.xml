<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.octacomm.sample.dao.mapper.GpsFileMapper">

	<sql id="condition">
        <where>
        	<if test="param.searchField != null and param.searchField != '' and param.searchWord != null and param.searchWord != ''">
       			and ${param.searchField} like '%${param.searchWord}%'
       		</if>
       		
       		<if test="param.constructionIdx != null and param.constructionIdx != ''">
       			and constructionIdx = #{param.constructionIdx}
       		</if>
       </where>
	</sql>
	
    <select id="getCountByParam" resultType="_int">
        SELECT COUNT(*) FROM TB_GPS_FILE
    	<include refid="condition"/>
    </select>
   
    <select id="getListByParam" resultType="GpsFile">
    	SELECT 
   		  *
    	FROM TB_GPS_FILE
    	<include refid="condition"/>
    	ORDER BY id ASC
        LIMIT #{pageSize} OFFSET #{startRow}
    </select>   
    
    
    <insert id="insertMulti" parameterType="java.util.List" >
	    INSERT INTO TB_GPS_FILE(
	          constructionIdx
	        , point
	        , xAxis
	        , yAxis
	        , zAxis
	        , code
	        , createDate
	        , lastModifiedDate
	    ) VALUES 
	    <foreach collection="list" item="item" separator=",">
	    (
	          #{item.constructionIdx}
	        , #{item.point}
	        , #{item.xAxis}
	        , #{item.yAxis}
	        , #{item.zAxis}
	        , #{item.code}
	        , now()
	        , now()
	    )
    </foreach>
</insert>
       
</mapper>