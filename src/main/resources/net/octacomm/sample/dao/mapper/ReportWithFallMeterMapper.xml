<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.octacomm.sample.dao.mapper.ReportWithFallMeterMapper">
	
	<insert id="insert" parameterType="ReportWithFallMeter">
	    INSERT INTO TB_REPORT
	    ( deviceIdx, currentDateTime, location, pileNo , pileStandard, drillingDepth , intrusionDepth, balance, connectLength, managedStandard, avgPenetrationValue, totalPenetrationValue, hammaT, fallMeter, pileType, method, totalConnectWidth, ultimateBearingCapacity, crossSection , hammaEfficiency , modulusElasticity 
	    	
	    	<choose>
	    		<when test="bigo != null and bigo != '' and bigo != null and bigo != ''">
	    		, bigo 
	    		</when>
	    		<when test="sprCol1 != null and sprCol1 != '' and sprCol1 != null and sprCol1 != ''">
	    		, sprCol1 
	    		</when>
	    	</choose>
	    	
	    	, createDate  )
	     VALUES 
	     ( #{deviceIdx}, #{currentDateTime}, #{location}, #{pileNo} , #{pileStandard},  #{drillingDepth} , #{intrusionDepth}, #{balance}, #{connectLength}, #{managedStandard}, #{avgPenetrationValue}, #{totalPenetrationValue}, #{hammaT}, #{fallMeter},  #{pileType}, #{method}, #{totalConnectWidth},  #{ultimateBearingCapacity} , #{crossSection} , #{hammaEfficiency} , #{modulusElasticity} 
	     	
	     	<choose>
	    		<when test="bigo != null and bigo != '' and bigo != null and bigo != ''">
	    		,  #{bigo} 
	    		</when>
	    		<when test="sprCol1 != null and sprCol1 != '' and sprCol1 != null and sprCol1 != ''">
	    		,  #{sprCol1} 
	    		</when>
	    	</choose>
	     	
	     	, now() )
	    <selectKey resultType="int" keyProperty="id" order="AFTER">
        SELECT LAST_INSERT_ID()
    	</selectKey>
    </insert>
	
	<insert id="insert2" parameterType="ReportWithFallMeter">
	    INSERT INTO TB_REPORT
	    ( deviceIdx, currentDateTime, location, pileNo , pileStandard, drillingDepth , intrusionDepth, balance, connectLength, managedStandard, avgPenetrationValue, totalPenetrationValue, hammaT, fallMeter, pileType, method, totalConnectWidth, ultimateBearingCapacity , crossSection , hammaEfficiency , modulusElasticity 
	    	<choose>
	    		<when test="bigo != null and bigo != '' and bigo != null and bigo != ''">
	    		, bigo 
	    		</when>
	    		<when test="sprCol1 != null and sprCol1 != '' and sprCol1 != null and sprCol1 != ''">
	    		, sprCol1 
	    		</when>
	    		
	    	</choose>
	    	, createDate  )
	     VALUES 
	     ( #{deviceIdx}, #{currentDateTime}, #{location}, #{pileNo} , #{pileStandard},  #{drillingDepth} , #{intrusionDepth}, #{balance}, #{connectLength}, #{managedStandard}, #{avgPenetrationValue}, #{totalPenetrationValue}, #{hammaT}, #{fallMeter},  #{pileType}, #{method}, #{totalConnectWidth},   #{ultimateBearingCapacity}, #{crossSection} , #{hammaEfficiency} , #{modulusElasticity} 
	     	<choose>
	    		<when test="bigo != null and bigo != '' and bigo != null and bigo != ''">
	    		,  #{bigo} 
	    		</when>
	    		<when test="sprCol1 != null and sprCol1 != '' and sprCol1 != null and sprCol1 != ''">
	    		,  #{sprCol1} 
	    		</when>
	    	</choose>
	     	, #{createDate} )
	    <selectKey resultType="int" keyProperty="id" order="AFTER">
        	SELECT LAST_INSERT_ID()
    	</selectKey>
    </insert>  
    
    
    <insert id="insertOrigin" parameterType="ReportWithFallMeter">
	    INSERT INTO TB_REPORT_ORIGIN
	    ( id, deviceIdx, currentDateTime, location, pileNo , pileStandard, drillingDepth , intrusionDepth, balance, connectLength, managedStandard, avgPenetrationValue, totalPenetrationValue, hammaT, fallMeter, pileType, method, totalConnectWidth, ultimateBearingCapacity, crossSection , hammaEfficiency , modulusElasticity 
	    	
	    	<choose>
	    		<when test="bigo != null and bigo != '' and bigo != null and bigo != ''">
	    		, bigo 
	    		</when>
	    		<when test="sprCol1 != null and sprCol1 != '' and sprCol1 != null and sprCol1 != ''">
	    		, sprCol1 
	    		</when>
	    	</choose>
	    	
	    	, createDate  )
	     VALUES 
	     ( #{id}, #{deviceIdx}, #{currentDateTime}, #{location}, #{pileNo} , #{pileStandard},  #{drillingDepth} , #{intrusionDepth}, #{balance}, #{connectLength}, #{managedStandard}, #{avgPenetrationValue}, #{totalPenetrationValue}, #{hammaT}, #{fallMeter},  #{pileType}, #{method}, #{totalConnectWidth},  #{ultimateBearingCapacity} , #{crossSection} , #{hammaEfficiency} , #{modulusElasticity} 

	     	<choose>
	    		<when test="bigo != null and bigo != '' and bigo != null and bigo != ''">
	    		,  #{bigo} 
	    		</when>
	    		<when test="sprCol1 != null and sprCol1 != '' and sprCol1 != null and sprCol1 != ''">
	    		,  #{sprCol1} 
	    		</when>
	    	</choose>
	     	, now() )
    </insert>
    
    
    <insert id="insert2Origin" parameterType="ReportWithFallMeter">
	    INSERT INTO TB_REPORT_ORIGIN
	    ( id, deviceIdx, currentDateTime, location, pileNo , pileStandard, drillingDepth , intrusionDepth, balance, connectLength, managedStandard, avgPenetrationValue, totalPenetrationValue, hammaT, fallMeter, pileType, method, totalConnectWidth, ultimateBearingCapacity , crossSection , hammaEfficiency , modulusElasticity 
	    	<choose>
	    		<when test="bigo != null and bigo != '' and bigo != null and bigo != ''">
	    		, bigo 
	    		</when>
	    		<when test="sprCol1 != null and sprCol1 != '' and sprCol1 != null and sprCol1 != ''">
	    		, sprCol1 
	    		</when>
	    	</choose>
	    	, createDate  )
	     VALUES 
	     ( #{id}, #{deviceIdx}, #{currentDateTime}, #{location}, #{pileNo} , #{pileStandard},  #{drillingDepth} , #{intrusionDepth}, #{balance}, #{connectLength}, #{managedStandard}, #{avgPenetrationValue}, #{totalPenetrationValue}, #{hammaT}, #{fallMeter},  #{pileType}, #{method}, #{totalConnectWidth},   #{ultimateBearingCapacity}, #{crossSection} , #{hammaEfficiency} , #{modulusElasticity} 
	     	<choose>
	    		<when test="bigo != null and bigo != '' and bigo != null and bigo != ''">
	    		,  #{bigo} 
	    		</when>
	    		<when test="sprCol1 != null and sprCol1 != '' and sprCol1 != null and sprCol1 != ''">
	    		,  #{sprCol1} 
	    		</when>
	    	</choose>
	     	, #{createDate} )
    </insert> 
</mapper>