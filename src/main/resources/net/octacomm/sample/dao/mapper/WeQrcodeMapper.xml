<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.octacomm.sample.dao.mapper.WeQrcodeMapper">
	
	
	<sql id="condition">
        <where>
        	qrSaveFilename IS NOT NULL
	       	<if test="param.searchField != null and param.searchField != '' and param.searchWord != null and param.searchWord != ''">
       			and ${param.searchField} like '%${param.searchWord}%'
       		</if>
       </where>
	</sql>
	
	<insert id="insert" parameterType="WeQrcode">
	    INSERT INTO TB_WE_QRCODE
	    ( 
	    	id, title, qrtype, qrtext, qrfilename
	    )
	    VALUES 
	    (
	        null, #{title}, #{qrtype}, #{qrtext}, #{qrfilename} 
	    )
	    <selectKey resultType="int" keyProperty="id" order="AFTER">
       		SELECT LAST_INSERT_ID()
    	</selectKey>
    </insert>
    
    
      <select id="getCountByParam" resultType="_int">
        SELECT COUNT(*) FROM TB_WE_QRCODE
    	<include refid="condition"/>
    </select>
   
    <select id="getListByParam" resultType="WeQrcode">
    	SELECT * FROM TB_WE_QRCODE
    	<include refid="condition"/>
    	ORDER BY id DESC
        LIMIT #{pageSize} OFFSET #{startRow}
    </select> 
	
</mapper>