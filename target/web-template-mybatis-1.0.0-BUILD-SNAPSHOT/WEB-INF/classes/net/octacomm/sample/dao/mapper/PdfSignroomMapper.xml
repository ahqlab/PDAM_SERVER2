<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.octacomm.sample.dao.mapper.PdfSignroomMapper">

    
    <insert id="registAll" parameterType="java.util.List" >
	    INSERT INTO TB_PDF_SIGNROOM
	    (
	          constructionIdx
	        , seq
	        , approver
	        , creator
	        , createDate
	    ) VALUES 
	    <foreach collection="list" item="item" separator=",">
	    (
	          #{item.constructionIdx}
	        , #{item.seq}
	        , #{item.approver}
	        , #{item.creator}
	        , now()
	    )
    	</foreach>
	</insert>
	
	
	<update id="updateAll" parameterType="java.util.List" >
	   
	    <foreach collection="list" item="item" separator=";">
	    	UPDATE TB_PDF_SIGNROOM
    		SET	   seq = #{item.seq}
    			 , approver = #{item.approver} 
    			 , modifyter = #{item.modifyter} 
    			 , lastModifiedDate = now()
    		WHERE  id = #{item.id}
    	</foreach>
    
	</update>
       
</mapper>